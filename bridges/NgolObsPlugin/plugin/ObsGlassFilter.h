#pragma once

// 映っている絵を実際の板に切り分け、3D で回しながら飛ばす種別。
//
// 画素の式で解く道（ngol_shader）とは別物。あちらは画面のどの点が
// どの破片から来たかを逆に解くので、板が前後に重なることも、
// 厚みも、正しい前後関係も表せない。こちらは破片を物として並べて描く。
//
// 割れた瞬間の絵を控え、それを板に貼る。以後その絵は変わらない。
namespace ObsGlassFilter {

// ホストへ種別を差し出す。obs_module_load から 1 度だけ呼ぶ。
void Register();

}
