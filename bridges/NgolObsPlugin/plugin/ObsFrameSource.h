#pragma once

// 名前を付けた場所に置かれた絵を、そのままソースとして出す種別。
//
// 素材をファイルで受け取る道は、渡されたものが固まる。置き換えるには
// 作り直して読み込み直すことになる。こちらは置き場を毎フレーム見るので、
// 置いた側が絵を差し替えると次の 1 枚から変わる。
//
// 置いた側が居なくても、途中で消えても、ここは落ちない。
// 何も無ければ何も描かず、大きさを 0 と答える。
namespace ObsFrameSource {

// ホストへ種別を差し出す。obs_module_load から 1 度だけ呼ぶ。
void Register();

}
